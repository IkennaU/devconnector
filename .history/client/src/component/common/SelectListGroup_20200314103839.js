import React from "react";
import classnames from "classnames";
import PropTypes from "prop-types";

function SelectListGroup({ name, placeholder, value, error, info, onChange }) {
  return <div></div>;
}

export default SelectListGroup;
